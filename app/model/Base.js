/* Sencha Cmd:
 TabletApp>sencha generate model -n Base -f id */

Ext.define('TabletApp.model.Base', {
    extend: 'Ext.data.Model',
    
    fields: [
        { name: 'id', type: 'auto' }

    ]
});
